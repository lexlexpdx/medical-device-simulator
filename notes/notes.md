# Patient medical device simulator

## Python environment

- Create python environment
  - `python3 -m venv venv`
  - Activate environment with `source venv/bin/activate`
  - Install MQTT library
    - `pip install paho-mqtt`

## Patient monitor simulator

- `simulator/monitor.py`
